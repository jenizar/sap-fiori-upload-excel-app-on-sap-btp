projection;
strict ( 2 );
use draft;

use side effects;

define behavior for YUPL_C_XL_FILE alias XLHead
{
  use create;
  use update;
  use delete;

  use action uploadExcelData;

  use action Activate;
  use action Edit;
  use action Discard;
  use action Resume;
  use action Prepare;

  use association _XLData { create; with draft; }
}

define behavior for YUPL_C_XL_DATA alias XLData
{
  //use update;
  //use delete;

  use association _XLFile { with draft; }

  use action processData;


}
